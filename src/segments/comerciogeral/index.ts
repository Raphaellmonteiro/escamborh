// Comércio Geral — segmento de varejo genérico
// Este segmento usa inteiramente as telas compartilhadas (POSScreen, OrdersScreen,
// ProductsScreen, EstoqueScreen, DashboardScreen, etc.) sem telas exclusivas.
// Toda a configuração de labels, emojis e flags está em src/config/segmentos.ts.